# Mapping of metadata format names to dicts with the keys
metadata_keys = {'micromanager': _micromananger_keys}

# Micromanager metadata keys on the imagingDB
_micromananger_keys = {
                      'key': 'MicroManagerMetadata'
                      'pixel_size': 'PixelSizeUm',
                      'xpos_um': 'XPositionUm',
                      'ypos_um': 'YPositionUm',
                      'zpos_um': 'ZPositionUm'
                     }
